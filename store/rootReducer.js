import { combineReducers } from 'redux';
// Import các reducer của bạn ở đây
// Ví dụ: import counterReducer from './counterReducer';

const rootReducer = combineReducers({
  // Định nghĩa các reducer của bạn ở đây
  // Ví dụ: counter: counterReducer,
});

export default rootReducer;